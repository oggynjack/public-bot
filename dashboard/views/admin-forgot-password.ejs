<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - PremiumPlus</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .forgot-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            max-width: 450px;
            width: 100%;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .forgot-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #f59e0b, #dc2626);
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #1f2937;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .header p {
            color: #6b7280;
            font-size: 16px;
            line-height: 1.5;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #374151;
            font-weight: 500;
            font-size: 14px;
        }

        .form-group input {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e5e7eb;
            border-radius: 10px;
            font-size: 16px;
            transition: all 0.3s ease;
            background-color: #f9fafb;
        }

        .form-group input:focus {
            outline: none;
            border-color: #f59e0b;
            background-color: white;
            box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
        }

        .btn {
            width: 100%;
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            color: white;
            padding: 14px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 20px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(245, 158, 11, 0.3);
        }

        .btn:active {
            transform: translateY(0);
        }

        .error {
            background-color: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
            padding: 12px 16px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .success {
            background-color: #f0fdf4;
            border: 1px solid #bbf7d0;
            color: #166534;
            padding: 12px 16px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .info-box {
            background: #f0f9ff;
            border: 1px solid #bae6fd;
            border-radius: 10px;
            padding: 16px;
            margin-bottom: 25px;
        }

        .info-box h3 {
            color: #0369a1;
            font-size: 16px;
            margin-bottom: 8px;
        }

        .info-box p {
            color: #0284c7;
            font-size: 14px;
            line-height: 1.5;
        }

        .back-link {
            text-align: center;
            margin-top: 20px;
        }

        .back-link a {
            color: #6b7280;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        .back-link a:hover {
            color: #3b82f6;
        }

        @media (max-width: 480px) {
            .forgot-container {
                padding: 30px 20px;
                margin: 10px;
            }

            .header h1 {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="forgot-container">
        <div class="header">
            <h1>🔐 Reset Password</h1>
            <p>Enter your admin email to reset your password</p>
        </div>

        <div class="info-box">
            <h3>📧 Password Reset Process</h3>
            <p>We'll send a verification code to your registered admin email. You can reset your password up to 3 times per day.</p>
        </div>

        <% if (error) { %>
            <div class="error">
                <span>⚠️</span>
                <span>
                    <% if (error === 'not_found') { %>
                        Email not found in admin system.
                    <% } else if (error === 'admin_access_only') { %>
                        <strong>🚫 ADMIN ACCESS ONLY</strong><br>
                        This email is not authorized by the master admin for password reset.
                    <% } else if (error === 'not_registered') { %>
                        Admin account not fully registered yet.
                    <% } else if (error === 'limit_exceeded') { %>
                        Daily password reset limit exceeded (3 attempts per day).
                    <% } else if (error === 'email_failed') { %>
                        Failed to send reset email. Please try again.
                    <% } else { %>
                        An error occurred. Please try again.
                    <% } %>
                </span>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="success">
                <span>✅</span>
                <span>
                    <% if (success === 'code_sent') { %>
                        Password reset code sent to your email!
                    <% } %>
                </span>
            </div>
        <% } %>

        <form action="/admin/forgot-password" method="POST">
            <div class="form-group">
                <label for="email">Admin Email</label>
                <input type="email" id="email" name="email" required placeholder="admin@example.com" autocomplete="email">
            </div>

            <button type="submit" class="btn">
                📨 Send Reset Code
            </button>
        </form>

        <div class="back-link">
            <a href="/admin/login">← Back to Login</a>
        </div>
    </div>

    <script>
        // Auto-focus on email input
        document.getElementById('email').focus();
    </script>
</body>
</html>
